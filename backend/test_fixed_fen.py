#!/usr/bin/env python3\n\nimport sys\nsys.path.insert(0, '/home/marblemaster/Desktop/Cursor/mvp1')\nfrom backend.etl.agents.game_search_agent import find_games_by_criteria\n\n# Test with starting position FEN\nstarting_fen = 'rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1'\nprint(f'Testing starting FEN: {starting_fen}')\nresults = find_games_by_criteria(fen_to_match=starting_fen, limit=3)\nprint(f'Found {len(results)} results')\nif results and len(results) > 0 and 'white_player' in results[0]:\n    for i, result in enumerate(results[:2]):\n        print(f'Game {i+1}: {result.get(\"white_player\")} vs {result.get(\"black_player\")}')\nelse:\n    print(f'Results: {results}')\n\n# Test with the user's FEN (after 1.e4)\nuser_fen = 'rnbqkbnr/pppppppp/8/8/4P3/8/PPPP1PPP/RNBQKBNR b KQkq - 0 1'\nprint(f'\\nTesting user FEN: {user_fen}')\nresults2 = find_games_by_criteria(fen_to_match=user_fen, limit=3)\nprint(f'Found {len(results2)} results')\nif results2 and len(results2) > 0 and 'white_player' in results2[0]:\n    for i, result in enumerate(results2[:2]):\n        print(f'Game {i+1}: {result.get(\"white_player\")} vs {result.get(\"black_player\")}')\nelse:\n    print(f'Results: {results2}')\n 